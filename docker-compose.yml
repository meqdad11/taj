version: '3.8'

services:
  bot:
    build: .
    container_name: download-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./downloads:/app/downloads
    ports:
      - "8080:8080"
